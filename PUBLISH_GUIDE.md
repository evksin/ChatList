# Полное руководство по публикации ChatList

Это руководство содержит пошаговые инструкции для публикации ChatList на GitHub Release и GitHub Pages.

## 📋 Содержание

1. [Подготовка к релизу](#подготовка-к-релизу)
2. [Публикация на GitHub Release](#публикация-на-github-release)
3. [Настройка GitHub Pages](#настройка-github-pages)
4. [Автоматизация](#автоматизация)

---

## Подготовка к релизу

### Шаг 1: Обновление версии

1. Откройте файл `version.py`
2. Обновите версию (например, `__version__ = "1.0.1"`)
3. Закоммитьте изменения:
   ```powershell
   git add version.py
   git commit -m "Bump version to 1.0.1"
   ```

### Шаг 2: Обновление CHANGELOG.md

1. Откройте `CHANGELOG.md`
2. Добавьте новую секцию с версией и датой
3. Заполните изменения (Добавлено, Изменено, Исправлено)
4. Закоммитьте:
   ```powershell
   git add CHANGELOG.md
   git commit -m "Update changelog for v1.0.1"
   ```

### Шаг 3: Обновление Release Notes

1. Откройте `RELEASE_NOTES_TEMPLATE.md`
2. Обновите версию и дату
3. Заполните информацию о релизе
4. Сохраните файл

### Шаг 4: Сборка приложения

```powershell
# Полная сборка (исполняемый файл + инсталлятор)
.\build_all.ps1
```

Или по отдельности:

```powershell
# Только исполняемый файл
.\build.ps1

# Только инсталлятор
.\build_installer.ps1
```

### Шаг 5: Тестирование

1. Установите приложение из `installer\ChatList-Setup-v{version}.exe`
2. Проверьте, что всё работает корректно
3. Убедитесь, что нет ошибок

---

## Публикация на GitHub Release

### Способ 1: Автоматический (рекомендуется)

Используйте скрипт `create_release.ps1`:

```powershell
# Проверка (dry run)
.\create_release.ps1 -DryRun

# Создание релиза
.\create_release.ps1
```

Скрипт автоматически:

- Прочитает версию из `version.py`
- Создаст тег версии
- Загрузит файлы на GitHub
- Создаст релиз с описанием

### Способ 2: Через веб-интерфейс GitHub

1. **Создайте тег:**

   ```powershell
   $version = python -c "from version import __version__; print(__version__)"
   git tag -a "v$version" -m "Release version $version"
   git push origin "v$version"
   ```

2. **Создайте Release:**
   - Перейдите на `https://github.com/yourusername/chatlist/releases`
   - Нажмите **"Draft a new release"**
   - Выберите тег `v{version}`
   - Заполните заголовок: `ChatList v{version}`
   - Вставьте содержимое из `RELEASE_NOTES_TEMPLATE.md`
   - Загрузите файлы:
     - `installer\ChatList-Setup-v{version}.exe`
     - `dist\ChatList-v{version}.exe` (опционально)
   - Нажмите **"Publish release"**

### Способ 3: Через GitHub CLI

```powershell
# Установите GitHub CLI (если не установлен)
winget install GitHub.cli

# Авторизуйтесь
gh auth login

# Создайте релиз
$version = python -c "from version import __version__; print(__version__)"
$notes = Get-Content "RELEASE_NOTES_TEMPLATE.md" -Raw

gh release create "v$version" `
    "installer\ChatList-Setup-v$version.exe" `
    "dist\ChatList-v$version.exe" `
    --title "ChatList v$version" `
    --notes "$notes"
```

---

## Настройка GitHub Pages

### Шаг 1: Подготовка файлов

1. **Вариант A: Использовать корневую папку**

   - Файл `index.html` должен быть в корне репозитория

2. **Вариант B: Использовать папку docs/ (рекомендуется)**

   ```powershell
   # Создайте папку docs
   New-Item -ItemType Directory -Path "docs" -Force

   # Скопируйте index.html в docs
   Copy-Item "index.html" "docs\index.html"
   ```

### Шаг 2: Обновление ссылок в index.html

Откройте `index.html` и замените:

- `yourusername` на ваш GitHub username
- `chatlist` на название вашего репозитория

Например:

```html
<!-- Было -->
<a href="https://github.com/yourusername/chatlist/releases/latest">
  <!-- Стало -->
  <a href="https://github.com/username/chatlist/releases/latest"></a
></a>
```

### Шаг 3: Настройка GitHub Pages

1. Перейдите в **Settings** → **Pages** вашего репозитория
2. В разделе **Source**:
   - **Branch**: `main` (или `master`)
   - **Folder**: `/ (root)` или `/docs` (в зависимости от выбранного варианта)
3. Нажмите **Save**

### Шаг 4: Публикация

```powershell
# Добавьте файлы
git add index.html
# Или если используете docs/
git add docs/index.html

# Закоммитьте
git commit -m "Add landing page for GitHub Pages"

# Отправьте на GitHub
git push
```

GitHub автоматически опубликует сайт через 1-2 минуты.

Ваш сайт будет доступен по адресу:

```
https://yourusername.github.io/chatlist/
```

### Шаг 5: Обновление сайта

При каждом обновлении `index.html`:

```powershell
git add index.html
git commit -m "Update landing page"
git push
```

GitHub автоматически обновит сайт.

---

## Автоматизация

### GitHub Actions для автоматического релиза

Файл `.github/workflows/release.yml` уже создан. Он автоматически:

1. Собирает приложение при создании тега
2. Создаёт релиз на GitHub
3. Загружает файлы

**Использование:**

```powershell
# Создайте тег
$version = python -c "from version import __version__; print(__version__)"
git tag -a "v$version" -m "Release version $version"
git push origin "v$version"
```

GitHub Actions автоматически запустится и создаст релиз.

**Примечание:** Для работы GitHub Actions требуется, чтобы Inno Setup был доступен в CI. Возможно, потребуется настроить установку Inno Setup в workflow или использовать только исполняемый файл.

---

## Чеклист перед публикацией

- [ ] Версия обновлена в `version.py`
- [ ] `CHANGELOG.md` обновлён
- [ ] `RELEASE_NOTES_TEMPLATE.md` заполнен
- [ ] Приложение собрано и протестировано
- [ ] `index.html` обновлён с правильными ссылками
- [ ] Все изменения закоммичены и запушены
- [ ] Тег версии создан
- [ ] Release создан на GitHub
- [ ] GitHub Pages настроен и работает

---

## Полезные ссылки

- [GitHub Releases Documentation](https://docs.github.com/en/repositories/releasing-projects-on-github)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [GitHub CLI Documentation](https://cli.github.com/manual/)
- [Semantic Versioning](https://semver.org/)

---

## Поддержка

Если возникли проблемы:

1. Проверьте логи GitHub Actions (если используете автоматизацию)
2. Убедитесь, что все файлы закоммичены
3. Проверьте права доступа к репозиторию
4. Создайте Issue на GitHub для получения помощи
