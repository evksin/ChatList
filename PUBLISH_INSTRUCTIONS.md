# ✅ Инструкция по публикации ChatList

## 🎉 Что уже сделано:

1. ✅ Обновлён `index.html` с правильными ссылками (evksin/ChatList)
2. ✅ Все файлы закоммичены и отправлены на GitHub
3. ✅ Создан тег `v1.0.0` и отправлен на GitHub

## 📦 Следующие шаги:

### 1. Создание GitHub Release (через веб-интерфейс)

1. Перейдите на страницу репозитория:

   ```
   https://github.com/evksin/ChatList/releases
   ```

2. Нажмите **"Draft a new release"** или **"Create a new release"**

3. Заполните форму:

   - **Tag version**: Выберите `v1.0.0` (или создайте новый)
   - **Release title**: `ChatList v1.0.0`
   - **Description**: Скопируйте содержимое из файла `RELEASE_NOTES_TEMPLATE.md`
     - Откройте `RELEASE_NOTES_TEMPLATE.md`
     - Замените `YOUR_USERNAME` на `evksin` и `YOUR_REPO` на `ChatList`
     - Замените `[ДД.ММ.ГГГГ]` на текущую дату
     - Скопируйте весь текст в поле Description

4. Загрузите файлы:

   - **Installer**: Перетащите файл `installer\ChatList-Setup-v1.0.0.exe`
   - **Executable** (опционально): Перетащите файл `dist\ChatList-v1.0.0.exe`

5. Нажмите **"Publish release"**

### 2. Настройка GitHub Pages через GitHub Actions

1. Перейдите в настройки репозитория:

   ```
   https://github.com/evksin/ChatList/settings/pages
   ```

2. В разделе **Build and deployment**:

   - **Source**: Выберите **GitHub Actions**

3. Нажмите **Save** (если нужно)

4. GitHub Actions автоматически запустится при следующем push в ветку `main`

5. Чтобы запустить вручную:

   - Перейдите в **Actions** → **Deploy GitHub Pages**
   - Нажмите **Run workflow** → **Run workflow**

6. После успешного выполнения workflow ваш сайт будет доступен по адресу:
   ```
   https://evksin.github.io/ChatList/
   ```

**Примечание:** Workflow уже создан в `.github/workflows/pages.yml` и будет автоматически публиковать сайт при каждом изменении в ветке `main`.

## 🔗 Полезные ссылки:

- **Репозиторий**: https://github.com/evksin/ChatList
- **Releases**: https://github.com/evksin/ChatList/releases
- **GitHub Pages**: https://evksin.github.io/ChatList/ (после настройки)
- **Settings → Pages**: https://github.com/evksin/ChatList/settings/pages

## 📝 Примечания:

- Если у вас установлен GitHub CLI, вы можете использовать скрипт `create_release.ps1` для автоматического создания релиза
- После настройки GitHub Pages, сайт будет автоматически обновляться при каждом изменении `index.html` в ветке `main`
- Для будущих релизов используйте скрипт `create_release.ps1` или следуйте той же процедуре

## ✨ Готово!

После выполнения этих шагов ваше приложение будет:

- ✅ Опубликовано на GitHub Release
- ✅ Доступно через GitHub Pages
- ✅ Готово к распространению
